<td><%= image_tag("/assets/#{show['url']}", :alt => "image") %></td>
<td><%= show['title'] %></td>
<td><%= show['url'] %></td>
<td><%= link_to 'Edit', edit_show_path(show) %></td>
<td><%= link_to 'Delete', show_path(show),
  method: :delete,
  data: { confirm: 'Are you sure?' } %></td>
<td><%= link_to 'Show', show_path(show) %></td>
